import React from 'react';
import AppBar from 'material-ui/lib/app-bar';

const TopBar = () => (
	<AppBar title="React Sandbox" iconClassNameRight="muidocs-icon-navigation-expand-more" />
);

export default TopBar;